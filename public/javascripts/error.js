/*$(window).on('resize', () => {
    const ratio = 45 / ($(window).innerWidth() / $(window).innerHeight())
    $("h1").css({
        transform: "translate(-50%, -50%) rotate(-" + ratio + "deg)"
    })
});

$(window).on('load', () => {
    const ratio = 45 / ($(window).innerWidth() / $(window).innerHeight())
    $("h1").css({
        transform: "translate(-50%, -50%) rotate(-" + ratio + "deg)"
    })
});*/

